# Команда установки для воспроизводимого окружения: pip install -r requirements-dev.txt -c constraints.txt.

-r requirements.txt

# Инструменты разработки.
pytest>=7.4.0       # Тесты.
ruff>=0.1.0         # Линтер (быстрее и современнее flake8/black).
pre-commit>=3.5.0   # Хуки перед коммитом.
