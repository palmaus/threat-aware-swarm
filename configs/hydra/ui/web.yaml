host: 127.0.0.1
port: 8000
screen_size: 900
max_steps: 600
goal_radius: 3.0
seed: 0
fps: 20
policy_workers: 4
oracle_enabled: true
oracle_async: true
oracle_update_interval: 10
attention_stride: 4
compare: false
scene_root: scenarios
user_scene_root: scenarios/user
runs_root: runs

hydra:
  run:
    dir: .
  output_subdir: null
  job_logging:
    root:
      level: INFO
