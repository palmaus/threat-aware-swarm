env:
  control_mode: waypoint
  physics_ticks_per_action: 4

ppo:
  cnn_extractor: advanced
  cnn_features_dim: 512
  learning_rate: 1e-4
  net_arch: [512, 256, 128]
  ent_coef: 0.05
  target_kl: 0.05
  clip_range: 0.3
