# @package _global_

defaults:
  - _self_

env: {}
policies:
  - baseline:flow_field
  - baseline:astar_grid
scenes:
  - preset:sanity
seeds: [0, 1, 2]
success_threshold: 0.5
max_steps: 600
goal_radius: 3.0
oracle_enabled: true
deterministic: true
out_dir: ""
out_name: ""
model: null

hydra:
  run:
    dir: .
  output_subdir: null
  job_logging:
    root:
      level: INFO
