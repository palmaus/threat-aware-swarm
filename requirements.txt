# --- Core Math & Physics (Математика и Векторы) ---
numpy>=1.24.0
scipy>=1.10.0

# --- Geometry & Graphs (Геометрия и Графы) ---
shapely>=2.0.0      # Для точных коллизий и зон
networkx>=3.0       # Для расчета графа связи и связности

# --- Visualization (Визуализация) ---
matplotlib>=3.7.0   # Графики и отладка
pygame>=2.5.0       # Живая симуляция

# --- RL & Environment Standards (Стандарты среды) ---
gymnasium>=0.29.0   # Базовый API среды
pettingzoo>=1.24.0  # Стандарт для Multi-Agent RL

# --- Configuration (Конфигурация) ---
hydra-core>=1.3.0   # Управление конфигами
omegaconf>=2.3.0

# --- Machine Learning (PyTorch) ---
# Базовая установка. Если нужна поддержка CUDA (GPU),
# лучше ставить отдельно командой с pytorch.org
torch>=2.0.0
shimmy >=1.6.0      # Обертка для совместимости Gymnasium и Stable Baselines3
stable-baselines3 >=2.0.0
tensorboard>=2.10.0
supersuit     # Преобразования для Multi-Agent сред

# --- Dev Tools (Качество кода и тесты) ---
pytest>=7.4.0       # Тесты
ruff>=0.1.0         # Линтер (быстрее и современнее flake8/black)
pre-commit>=3.5.0   # Хуки перед коммитом
tqdm>=4.66.0        # Прогресс-бары для обучения