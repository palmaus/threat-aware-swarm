# Команда установки для воспроизводимого окружения: pip install -r requirements.txt -c constraints.txt.

# Математика и векторы.
numpy>=1.24.0
scipy>=1.10.0
numba>=0.59.0     # JIT-ускорение A*/Dijkstra в оракуле и бейзлайнах.

# Геометрия и графы.
shapely>=2.0.0      # Точные коллизии и зоны.
networkx>=3.0       # Расчет графа связи и связности.

# Визуализация.
matplotlib>=3.7.0   # Графики и отладка.
pygame>=2.5.0       # Живая симуляция.
pillow>=10.0.0      # Кодирование PNG для веб-интерфейса.

# Стандарты среды.
gymnasium>=0.29.0   # Базовый API среды.
pettingzoo>=1.24.0  # Стандарт для мультиагентного RL.

# Конфигурация.
hydra-core>=1.3.0   # Управление конфигами.
omegaconf>=2.3.0
hydra-joblib-launcher>=1.2.0  # Multirun через Joblib (Hydra launcher).

# Трекинг и реестр моделей.
mlflow>=2.12.0      # Реестр моделей и трекинг.
clearml>=1.15.0     # ClearML tracking (основной трекинг).

# Масштабирование экспериментов.

# Машинное обучение (PyTorch).
# Базовая установка без CUDA; для GPU используйте отдельную установку с pytorch.org.
torch>=2.0.0
captum>=0.7.0      # Интерпретируемость нейросетей (saliency maps).
shimmy >=1.6.0      # Совместимость Gymnasium и Stable Baselines3.
stable-baselines3 >=2.0.0
sb3-contrib>=2.0.0
tensorboard>=2.10.0
supersuit     # Преобразования для мультиагентных сред.

# Утилиты и обучение.
tqdm>=4.66.0        # Прогресс-бары для обучения.
optuna>=3.6.1       # Байесовская оптимизация и подбор гиперпараметров.
fastapi>=0.115.0    # Веб-интерфейс.
uvicorn>=0.30.0     # Сервер веб-интерфейса.
websockets>=12.0    # WebSocket transport для Uvicorn.
psutil>=5.9.0       # Системные метрики (CPU/RAM/RSS).
msgpack>=1.0.0      # Бинарная телеметрия для Web UI.
imageio>=2.34.0     # Экспорт GIF для прогонов/визуализации.
onnx>=1.16.0        # Экспорт ONNX для edge deployment.
onnxruntime>=1.17.0 # Инференс/квантизация ONNX (CPU).
diagrams>=0.25.1    # Architecture-as-code диаграммы.
torchview>=0.2.7    # Граф нейросети.
graphviz>=0.20.3    # Python-обёртка Graphviz (нужен системный graphviz).
